# Implementation Plans

Generated by the improve skill on 2026-08-26 against commit `dff4ba2`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

Verification baseline before plan 001: only `node --check export-issues.js`.
After 001: `npm test` (Node's built-in `node --test`). `npm run export` is
interactive (headed browser + live Jira) and is never a verification gate.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Test baseline: export functions, `node --test` suite | P1 | M | — | DONE |
| 007 | Non-zero exit on failure; clear output dir safely | P2 | S | — | DONE |
| 009 | package.json / lockfile hygiene | P3 | S | — | DONE |
| 002 | Paginate search; export orphaned issues at top level | P1 | S | 001 | DONE |
| 003 | Render `link` marks and inlineCard URLs | P1 | S | 001 | DONE |
| 004 | Detect sub-tasks by `issuetype.subtask` flag | P1 | S | 001 | DONE |
| 005 | Fix index and parent links in generated Markdown | P2 | S | 001, 004 | DONE |
| 006 | Extend ADF coverage (tables, nested lists, panels, …) | P2 | M | 001, 003 | DONE |
| 008 | Encode query params; hoist JQL to `JIRA_JQL` env | P3 | S | 002 | DONE |
| 010 | Direction: persist browser session (storageState) | P3 | M | 007 | DONE (Step 6 live verification: not run) |
| 011 | Direction: configurable query, limits, extra fields | P3 | S–M | 008 | TODO |
| 012 | Direction: download attachments | P3 | M | 002, 006 | DONE (Step 2 spike skipped by operator; branch-agnostic join — see note) |
| 013 | Direction: rate-limit backoff and adaptive throttling | P2 | M | 001, 002, 012 | DONE (live 429 not reproduced; stubbed) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first: it makes `descriptionToMd`, `generatePath`, `generateIssueFiles`,
  `fetchAllParentIssues` importable and adds characterization tests that
  002–006 flip to correct behaviour. Without it those fixes are only
  verifiable by a live Jira login.
- 007 and 009 are independent of 001 and may run in parallel with it.
- 005 depends on 004 because leaf-vs-folder placement decides which relative
  parent path is correct.
- 006 depends on 003 because both edit `processNode`; 012 depends on 006's
  `attachment:<id>` placeholders.
- 008 edits the `searchIssues` function 002 introduces; 011 builds on the
  `JIRA_JQL` env var 008 introduces.

## Plan 012 note: the attachment join key

Step 2 of plan 012 was a live-Jira spike to decide whether to join ADF media
nodes to REST attachment records on a media UUID (branch 1), on the filename
(branch 2), or not at all (branch 3). The operator chose to skip the spike in
favour of a design that covers all three: `attachmentKeys` maps each attachment
under its REST id, its filename, and any UUID-shaped field on the record, and
`rewriteAttachmentLinks` also tries the placeholder's alt text as a key. Every
attachment is downloaded and listed under `## Attachments` regardless, so
branch 3's guarantee holds even when no placeholder resolves. Unresolved
placeholders are left as `attachment:<id>`.

## Findings considered and rejected

- O(n²) child lookup in `generateIssueFiles` (`Object.values(map).filter` per
  issue) and sequential parent fetches: n is tens of issues; not worth doing.
- Security: filenames are restricted to `[a-z0-9-]`, no secrets in repo,
  `JIRA_URL` from `.env` is the intended trust boundary. No findings.
- `npm audit`: not run (no `node_modules` at audit time). Run after 009 lands.
- `new Date(undefined).toISOString()` throws on missing `created`/`updated`:
  both fields are always requested and always present on Jira issues; low
  value, not planned.

## Not audited

Live Jira API behaviour (no run performed), Playwright version currency.
